{
  "hash": "503d52fad878d03dad80eca68003e649",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"*ggplot2* tutorial I\"\nformat:\n  html:\n    code-fold: false\n---\n\n## About\n\nThis page provides some activities for beginning users of the *ggplot2* [@ggplot2] package.\n\n## Get ready\n\n### Create a new project\n\nLet's open project to capture our work with *ggplot2*.\n\nI suggest calling the project `ggplot-2026-09-18` or something similar.\n\n:::: {.columns}\n::: {.column width=50%}\n![From File Menu](../include/img/file-menu.png){fig-align=\"center\" #fig-file-menu-new-project width=\"75%\"}\n:::\n::: {.column width=50%}\n![From Project Manager Tab](../include/img/new-project.png){fig-align=\"center\" #fig-new-project-project-manager width=\"75%\"}\n:::\n::::\n\n---\n\n![Create project in a new directory](../include/img/create-project-new-directory.png){#fig-create-project-new-directory width=\"50%\"}\n\n---\n\n![Project Type: Quarto Project](../include/img/create-project-type-quarto.png){#fig-create-project-type-quarto width=\"50%\"}\n\n---\n\n![Create Quarto Project](../include/img/new-ggplot2-project.png){#fig-new-ggplot2-project width=\"50%\"}\n\n## Edit the document\n\nLet's customize this document for our use.\n\n### Change the metadata\n\nBy default, the metadata at the top of your document looks like this:\n\n```yaml\n---\ntitle: \"psych-490-ggplot-2026-09-18\"\n---\n```\n\nAdd an author field so that your metadata looks like this:\n\n```yaml\ntitle: \"psych-490-ggplot-2026-09-18\"\nauthor:  \"Jerome Doe\"\n```\n\nFinally, add the following so that the rendered HTML file includes digital versions of any figures you render.\nThis will make it easy to share your work with others.\n\n```yaml\nformat:\n  html:\n    self_contained: true\n```\n\nThe full header should look like this:\n\n```yaml\n---\ntitle: \"psych-490-ggplot-2026-09-18\"\nauthor: \"Your name\"\nformat:\n  html:\n    self_contained: true\n---\n```\n\n::: {.callout-important}\n## Save early and often\n\nI like to save my documents often.\nThat way I don't lose anything.\n\nMemorize your computer's save command.\nOn Mac OS it's command + S.\nOn Windows, it's ctrl + S.\n\nI also recommend checking the `Render on Save` box in RStudio.\nThat way every time you save, the document re-renders and opens in your browser.\n:::\n\n## Install *ggplot2*\n\nBase R has many useful functions, but *ggplot2* is not one of them.\nSo, you will need to install it using the `install.packages()` function.\n\nSwitch to the R console, and install the package as follows:\n\n```r\ninstall.packages(\"ggplot2\")\n```\n\nYou'll only need to do this once.\n\n## Quarto as a lab notebook\n\nWe're going to use Quarto as a lab notebook.\n\nEach time we start a new thing, we're going to start a new section in our document.\n\n### Add a `Setup` section\n\nThis is a good time to add a setup section to your document.\nThis section is where get ready to do real work.\n\nSo, add a chunk like this after your `About` section:\n\n````markdown\n## Setup\n````\n\nBelow the setup header, we're going to add an R code chunk like this:\n\n````{{r}}\nlibrary(ggplot2)\n````\n\nThe `library()` command brings the package into the computational environment^[This is akin to bringing something into your working memory.] and lets us call the other functions and commands in *ggplot2* by their \"first\" names.\n\n## Our first plot\n\nAdd a new section called `My First Plot` or `Figure 01` or something like that.\n\n````markdown\n## Figure 01\n````\n\nThen you might add some text explaining what the first plot is about. For example, you could describe the data being visualized and the purpose of the plot.\n\nHere, we're going to use the `diamonds` dataset the is built in to *ggplot2*.\n\nCreate an R chunk as follows:\n\n::: {#fig-plot-diamonds-price}\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = price) +\n  geom_histogram()\n````\n\nSimple *ggplot2* syntax to plot a histogram of the price variable in the diamonds dataset.\n:::\n\n::: {.callout-warning}\n## Syntax slip-ups\n\n90+% of frustrations with computers are because we don't tell them exactly what they need to know.\n\nA code chunk in Quarto starts with triple backticks and ends with triple backticks.\n\nThe `{r}` tells Quarto that the code chunk is written in R.\n\nQuarto understands other programming languages, so this is important.\n:::\n\nSave and render your Quarto document.\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stderr}\n\n```\n`stat_bin()` using `bins = 30`. Pick better value `binwidth`.\n```\n\n\n:::\n\n::: {.cell-output-display}\n![Output of code in @fig-plot-diamonds-price.](../include/fig/fig-diamonds-histogram-price-1.png){#fig-diamonds-histogram-price width=672}\n:::\n:::\n\n\n\nThe resulting web page should look something like this:\n\n![Quarto document rendered after Figure 01 code is executed.](../include/img/ggplot-tutorial-fig-01.png){width=\"50%\" #fig-01}\n\n### Unpacking *ggplot* syntax\n\nTo make a figure, we need a dataset formatted as a data frame. The `diamonds` dataset is already in the right format.\n\nIn our plotting code illustrated in @fig-plot-diamonds-price, we call the `ggplot()` function and specify the dataset to plot using the `data=diamonds` parameter^[A parameter is a value that we send to a function to help the function do the job we want it to do.].\n\nNext, we 'add' a layer to our figure using the `+` command.\nThat layer contains the 'aesthetics' or mapping between our dataset and some feature of the figure (e.g., x position, y position, fill color, etc.)\nWe specify these mappings using the `aes()` command.\nFor this specific figure, we're mapping x position on the figure to the `price` variable in the dataset.\n\nFinally, our figure needs a 'geom', a geometric object or format for the figure.\nWe have a single variable (`price`), and we want to plot a histogram of it, so we add (`+`) use the `geom_histogram()`.\n\n## Another variable\n\nLet's plot another variable from the `diamonds` datasets.\n\nCreate a new subsection called `Figure 02` in your Quarto document, then create a new code chunk in your Quarto document with the following code:\n\n:::: {#fig-diamonds-head}\n\n````{{r}}\nhead(diamonds)\n````\n\nCode to show the 'head' of the diamonds dataset.\n::::\n\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 6 × 10\n  carat cut       color clarity depth table price     x     y     z\n  <dbl> <ord>     <ord> <ord>   <dbl> <dbl> <int> <dbl> <dbl> <dbl>\n1  0.23 Ideal     E     SI2      61.5    55   326  3.95  3.98  2.43\n2  0.21 Premium   E     SI1      59.8    61   326  3.89  3.84  2.31\n3  0.23 Good      E     VS1      56.9    65   327  4.05  4.07  2.31\n4  0.29 Premium   I     VS2      62.4    58   334  4.2   4.23  2.63\n5  0.31 Good      J     SI2      63.3    58   335  4.34  4.35  2.75\n6  0.24 Very Good J     VVS2     62.8    57   336  3.94  3.96  2.48\n```\n\n\n:::\n:::\n\n\nThe `head()` command outputs the first few rows of a data frame. This is useful for quickly inspecting the structure and contents of the data.\n\nLet's make a histogram of the `carat` variable.\nMake a new R chunk in your Quarto file; copy the Figure 1 code to the chunk; change the `x` value in the `aes()` function to read `aes(x = carat )`.\n\n::: {#fig-diamonds-carat}\n\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = carat) +\n  geom_histogram()\n````\n\nCode to generate a histogram of the `carat` variable from the `diamonds` dataset.\n:::\n\nSave and render the Quarto document.\n\n\n::: {.cell}\n::: {.cell-output .cell-output-stderr}\n\n```\n`stat_bin()` using `bins = 30`. Pick better value `binwidth`.\n```\n\n\n:::\n\n::: {.cell-output-display}\n![Output of the code in @fig-diamonds-carat](../include/fig/fig-output-histogram-carat-1.png){#fig-output-histogram-carat width=672}\n:::\n:::\n\n\n## Violin plots\n\nThere are other 'geoms' we can use with continuous data like `price` and `carat`.\nA violin plot is one of them.\n\nAgain, make a new `Figure 03` section in your Quarto document.\nThen, make a new code chunk with the following:\n\n::: {#fig-violin-plots}\n\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = price, y = \"\") +\n  geom_violin()\n  \nggplot(data = diamonds) +\n  aes(x = carat, y = \"\") +\n  geom_violin()\n````\n\nCode to create violin plots of both the `price` and `carat` variables.\n:::\n\nYes, this should make two different violin plots.\n\nSave and render the Quarto document\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Output from the code chunk in @fig-violin-plots.](../include/fig/fig-diamonds-violins-carat-price-1.png){#fig-diamonds-violins-carat-price-1 width=672}\n:::\n\n::: {.cell-output-display}\n![Output from the code chunk in @fig-violin-plots.](../include/fig/fig-diamonds-violins-carat-price-2.png){#fig-diamonds-violins-carat-price-2 width=672}\n:::\n:::\n\n\nNotice that `geom_violin()` requires us to specify both `x` and `y` aesthetics even if we only want to plot one variable. \nWe tell *ggplot* to ignore the other mapping by setting `y=\"\"`.\n\n## Scatterplots\n\nLet's make a scatterplot of `price` and `carat` using `geom_point` and set the `x` and `y` aesthetics.\nThat will put `price` on the `x` or horizontal axis and `carat` on the `y` or vertical axis.\nYou could switch them; it doesn't really matter.\n\nMake a `Figure 04` section in your Quarto document, then add a code chunk with the following:\n\n::: {#fig-diamonds-scatterplot}\n\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = price, y = carat) +\n  geom_point()\n````\n  \nCode to create a scatterplot of the `price` and `carat` variables from the diamonds dataset.\n:::\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Output from the prior code chunk.](../include/fig/fig-diamonds-scatterplot-1.png){#fig-diamonds-scatterplot width=672}\n:::\n:::\n\n\n## Multiple violin plots\n\nIf you want to push yourself, add a Figure 05 section with the following code chunk.\nSave and render the document, then explain what's going on with the figure.\n\n::: {#fig-multiple-violin-plots}\n\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = price, y = cut, color = cut, fill = cut) +\n  geom_violin() +\n  xlab(\"Price in $\") +\n  ylab(\"Cut of diamond\") +\n  ggtitle(\"Price vs. Cut\")\n````\n\nCode to generate multiple violin plots, one for each value of the discrete (ordinal) variable `cut`. Note the use of new aesthetics `fill` and `color`, customized axis labels using `xlab()` and `ylab()` and a title using `ggtitle()`.\n:::\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Output from the prior code chunk.](../include/fig/fig-diamonds-multi-violins-1.png){#fig-diamonds-multi-violins width=672}\n:::\n:::\n\n\n## Combination plot\n\nLet's implement a version of the combination plot from the [Making and visualizing data tutorial](tutorial-making-data.html#violin-boxplot-scatter).\n\nCreate a `Figure 06` section in your Quarto document, add this chunk of code, save, and render.\n\n::: {#fig-code-diamonds-violin-boxplot}\n\n````{{r}}\nggplot(data = diamonds) +\n  aes(x = price, y = \"\") +\n  geom_violin() +\n  geom_boxplot(alpha = .4) +\n  ylab(\"\")\n````\n\nCode to generate combination plot of diamonds price data.\n:::\n\n\n::: {.cell}\n::: {.cell-output-display}\n![Output from the code in @fig-code-diamonds-violin-boxplot.](../include/fig/fig-diamonds-violin-boxplot-1.png){#fig-diamonds-violin-boxplot width=672}\n:::\n:::\n\n\n::: {.callout-important}\n## What are those new parameters?\n\nNote that there is an `alpha` parameter in the `geom_boxplot()` function in @fig-code-diamonds-violin-boxplot. What does it do?\n\nOne way to answer this is to create new versions of the figures with different values of `alpha`.\nYou could do this by creating new subsections in your Quarto document, say one like this:\n\n````markdown\n### `alpha = 1`\n\nThe following figure shows what happens to Figure 06 when we set `alpha` in `geom_boxplot` to 1.\n````\n\nThen, copy the Figure 06 code to a new code chunk but change the `alpha` value.\n\nSo, what does `alpha` do?\n:::\n\n## You did it!\n\nYou made a bunch of figures using *ggplot*!\nAnd you documented all of your work in a Quarto document.\nYou're one step closer to being a *ggplot* master.\n\nHere's a happy dance animated gif I found, but you can find one you like yourself at [giphy.com](https://giphy.com).\n\n![](https://media0.giphy.com/media/v1.Y2lkPTc5MGI3NjExZXJkbmd2dGZwODlxYjdwaTJ2NGN4djUwYXl2ZW03a2RzMW44MzNqayZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/EkPicGgIeyNAQ/giphy.gif)\n\n## References\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}